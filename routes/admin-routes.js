// v2

const express = require('express');
const router = express.Router();
const adminController = require('../controllers/admin-ctrl');

    // router.route('/admin')
    // .get(adminController.loadAdmin)
